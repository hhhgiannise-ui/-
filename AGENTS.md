# AGENTS.md — 项目工作规则

本文件是 AI 与协作者在本仓库内工作时必须遵守的规则。

## 硬性规则

1. **每次改动后必须创建 git commit**，禁止积压未提交的改动。
2. **每次改动后必须编写/更新相关测试**，并在交付前确保测试通过。
3. 禁止把敏感信息（密钥、Token、envId 之外的环境配置）写入代码并提交。
4. 代码保持精简：AI 生成的多余冗余代码应删除，不追求大而全。
5. 文档驱动：先有 PRD / 项目指南 / API 文档，再写代码；代码与文档保持一致。

## 输出与教学约定（学习者协作模式）

本项目是"边做边学"的教学式开发，AI 每轮协作必须遵守：

1. **输出按任务类型分级**（结构是骨架，内容与深度由 AI 按需发挥）：
   - 新功能开发：完整三块——① 我做了什么（步骤 + 原因，可复现）② 本轮重要命令（完整可复制）③ 你学到了什么 + 以后自己怎么做；另加关键设计决策讲解（为什么这么设计）
   - bug 排查：三块 + 重点输出"我是怎么分析的"——数据流梳理、矛盾点、候选原因、如何用事实排除假设
   - 小改动 / 部署 / 验证：简短说明，不凑三块
   - 讨论 / 答疑：纯内容输出，无固定格式
2. **指导用户操作时**：每步带上"这一步是干什么的、有什么意义"，不甩裸命令。
3. **每次修改必须 git 操作**（见硬性规则第 1 条），汇报时带上 commit 号，方便用户回看全流程。
4. 所有输出使用中文。

## 经验教训（踩坑记录）

- **`add()` 必须写 `collection.add({ data: {...} })`**：wx-server-sdk 的 add/update 参数都要把字段包在 `{ data: ... }` 里；传裸对象会静默插入"只有 _id 的空文档"且返回 ok，极易误判为写入成功（2026-09-12 analyzeMistake 排查实例）。
- **排查顺序：先本地、后云端；先对比、后怀疑环境**：遇"写入/查询结果不对"，第一步先在仓库内找同类**正常工作**的代码做调用姿势对比（差分调试，秒级、AI 自己可完成），本地排除后才让用户去控制台验证版本/环境（分钟级、成本高）。2026-09-12 复盘："记录只有 _id"证据出现时一次 diff 即可定位，实际先绕了 6 轮云端排查。

## 提交规范

- 格式：`<type>(<scope>): <描述>`
- type：feat / fix / docs / refactor / test / chore
- 示例：`feat(auth): 完成微信一键登录` / `docs(prd): 初版 PRD 定稿`

## 目录约定

- `docs/prd/` 需求文档（PRD）
- `docs/design/` 设计文档（数据库设计等）
- `docs/api/` 接口文档
- `plans/` 开发计划与路线图；完成后归档到 `plans/Archive/`
- `for_agent/` 给 AI 的指导文档（代码规范等）
- `miniprogram/` 小程序前端代码
- `cloudfunctions/` 云函数（后端逻辑）
